---
title: SQL (PLANNED)
sidebar: { order: 100 }
---

This is heavily inspired by C#'s linq and should be able to query arbitrary collections.

```rip
FROM users AS u
WHERE id = "42"
SELECT u.name, u.dob
```
