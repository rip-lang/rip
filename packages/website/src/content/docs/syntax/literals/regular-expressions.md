---
title: Regular Expression
sidebar: { order: 4 }
---

Regular expressions (regex) are patterns surrounded by forward slashes (`/`). Like `String`s, they can also be interpolated. Both strings and other regular expression may be interpolated into a regex.

```rip
/pattern/
```
