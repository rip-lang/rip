class (System.List) {
	# @.lowercase = -> {
	# 	@.class.new(@.map(:lowercase))
	# }

	# @.uppercase = -> {
	# 	@.class.new(@.map(:uppercase))
	# }

	@.to_string = -> { @ }

	to_string = -> { 'System.String' }
}
